You are Claude, acting as the lead developer/architect for an open-source scientific software release.

Repository context:
- You are in a project directory containing:
  - thesis.txt  (full prior thesis text; treat it as authoritative background and cite it when writing docs)
  - program_code/  (an unzipped legacy Python codebase with many scripts + data files)
- Goal: turn the legacy code into a clean, well-engineered, well-documented, reproducible, fast, open-source “polymer growth modelling software” suitable to accompany a scientific paper.
- Domain goal (chemistry + modeling):
  - Polymer chains grow one repeat unit at a time; in the ideal case lengths are Poisson distributed.
  - Rare side reactions occur, including “death/transfer” and “vampiric” coupling between dead and living chains.
  - We want to infer the probabilities/parameters of these reactions from an observed polymer length distribution.
  - Approach: an agent-based polymer growth simulator + parameter optimization using a genetic algorithm (and variants: selection strategies, island model, co-evolution/fitness-diversity driven co-evolution).
- User requirement:
  - Produce usable open-source software: clear CLI, optionally GUI, strong docs, tests, reproducibility, performance.
  - Must be easier to run than legacy code. Must be maintainable and paper-quality.

Your tasks:
1) Repository audit (read almost everything)
   - Walk the entire program_code/ tree.
   - Identify:
     - Entry points / main scripts
     - Simulation implementation (agent-based polymer growth)
     - Parameter definitions and bounds
     - Fitness function(s) and normalization
     - Genetic algorithm implementation(s): baseline GA, selection methods, island model, co-evolution, etc.
     - Any GUI code (likely incomplete)
     - Data formats: experimental distributions, generated data, preprocessing scripts
     - Plotting/analysis scripts
     - Hardcoded paths, global state, randomness handling, performance bottlenecks
   - Produce a short “map of the codebase”:
     - For each major file/module: purpose, inputs/outputs, dependencies, and whether it should be kept/refactored/replaced.
   - Do not skip files because they look “auxiliary”; many may be critical for workflow.

2) Define the target product (what “proper software” means)
   - Deliverables:
     A) A Python package (pip-installable) with a stable public API:
        - simulator: run polymer growth simulation given parameters and random seed
        - objective: compare simulated distribution to target distribution (fitness/cost)
        - optimizers: GA variants (baseline, island model) with consistent interface
        - io: load/save target distributions, results, configs
        - plotting/reporting: optional, separated from core computation
     B) A CLI:
        - `polymer-sim simulate --config config.yml --seed 123 --out results/`
        - `polymer-sim fit --target data/target.csv --optimizer island --config config.yml --out results/`
        - `polymer-sim benchmark ...` and `polymer-sim reproduce ...`
     C) Documentation:
        - README with quickstart
        - “Theory” page summarizing model and parameters (pull from thesis.txt)
        - “Reproducibility” page (seeds, deterministic runs, versioning)
        - Developer docs (architecture, adding new reaction types, adding new optimizers)
     D) Tests:
        - Unit tests for simulator invariants (monomer conservation logic where applicable, distribution validity)
        - Determinism tests (same seed → same result if stochastic source controlled)
        - Regression tests on small toy targets
     E) Performance:
        - Identify bottlenecks (likely per-step per-agent loops).
        - Provide acceleration plan (vectorization, Numba, multiprocessing, caching, reduced variance strategies).
     F) Packaging and CI:
        - pyproject.toml
        - ruff/black, mypy (or pyright)
        - GitHub Actions for tests/lint
        - License + citation (CITATION.cff)

3) Chemistry/model specifics you must preserve (read thesis.txt for details)
   - Simulator is stochastic, agent-based.
   - Parameters (as described in thesis.txt) include:
     - simulation_time, number_of_molecules, monomer_pool
     - pg (probability of growth), pd (probability of death), pdr (probability of dead reaction)
     - le (living exponent), de (death exponent), ln (living naked)
     - death_spawns_new_monomer (boolean)
   - Growth/death logic uses monomer_ratio scaling.
   - Vampiric coupling success probability depends on living and dead chain lengths with exponents/naked ratio.
   - Fitness compares simulated vs target length distributions; normalization may be min-max.
   - The thesis discusses issues with real data preprocessing and noise; the software should support:
     - fitting to synthetic data for validation
     - robust handling of noisy tails (optionally weighting, trimming, sigma/bin weighting)
   - Maintain ability to run with large molecule counts and/or repeated simulations to reduce variance.

4) Explicit refactor plan (produce an actionable roadmap)
   - After audit, propose a phased plan:
     Phase 0: make legacy runnable reliably (fix paths, config, seeds, environment)
     Phase 1: isolate “core simulator” into a module with tests + stable API
     Phase 2: isolate “fitness/objective” into a module; standardize distribution representation
     Phase 3: refactor optimizers into consistent interface; remove duplicated GA code
     Phase 4: add CLI + config system (YAML/TOML) + logging
     Phase 5: performance work (Numba/vectorization, multiprocessing, caching)
     Phase 6: docs + examples + release packaging
   - For each phase: list concrete commits/PRs (what files change, what tests added).

5) Performance and correctness requirements (non-negotiable)
   - Reproducibility:
     - Central RNG management (numpy Generator), seed passed everywhere.
     - Option to run multiple replicates per parameter set and average cost.
   - Speed:
     - Provide at least one accelerated backend:
       - Numba JIT for simulation loop OR vectorized stepping OR Cython (prefer Numba first).
     - Multiprocessing option for evaluating a GA population in parallel.
   - Correctness:
     - Validate distributions: non-negative counts, expected totals, monotonic constraints if any.
     - Ensure monomer accounting is consistent with model assumptions.
     - Guard against silent failure (nan costs, empty distributions).

6) UX requirements
   - Make it easy for a chemist to run:
     - Simple config file defining parameter bounds and experiment settings.
     - Clear outputs: best-fit parameters, cost trajectory, distribution plots saved to disk.
     - “one command” reproduce example.
   - GUI is optional unless legacy has one worth salvaging; if kept, keep it thin and delegate to the CLI/API.

7) What to output now
   - Start by producing:
     A) codebase inventory (tree + module summaries)
     B) list of current workflows (“how do I run it today?”)
     C) proposed new architecture (package layout + responsibilities)
     D) initial set of refactor PRs (Phase 0–1) with exact steps

Operational instruction:
- Do not assume a file is irrelevant. Open and skim nearly all Python scripts and key data/config files.
- When you find duplication, pick one canonical implementation and retire the rest behind a compatibility shim if needed.
- Use thesis.txt as the source of truth for the model description and parameter meaning; reflect that in docstrings and documentation.

Begin now with the audit: produce a high-signal map of program_code/ and identify the real “core” simulation + optimization loop(s).
